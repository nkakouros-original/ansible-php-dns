---

php_dns_script_template: php-dns.j2
# Use this variable to change the php-dns start script

php_dns_record_file: dns_records.json.j2
# Use this variable to change the dns db template

php_dns_service_template: php-dns.service.j2
# Use this variable to use a custom systemd service file

composer_global_packages:
  - name: yswery/dns
    version: v1.3
  - name: psr/log
