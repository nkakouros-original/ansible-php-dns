{
  {% for domain in php_dns_records %}
    "{{ domain['domain'] }}":
      {{ domain | removekeys(['domain']) | to_json }},
  {% endfor %}
  "comments": "{{ php_dns_records_comments | default('') }}"
}
